# Conventions

- When editing any file under `templates/`, run `npm run bootstrap` in the same change so this repo's own copies of those dotfiles (`.editorconfig`, `.gitignore`, `.prettierignore`) stay in sync with the templates. The repo bootstraps itself; a template edit that leaves the local dotfile stale is incomplete.
